name: Delete DEV branch release

on:
  pull_request:
    types:
      - closed

jobs:
  delete_dev_job:
    environment: Development
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
      - name: Delete temporary release from dev environment
        id: delete_dev
        uses: ./.github/actions/delete-dev-release
        with:
          k8s_cluster: ${{ secrets.AUTOGENERATED_NSCC_STORE_DEV_K8S_CLUSTER_NAME }}
          k8s_cluster_cert: ${{ secrets.AUTOGENERATED_NSCC_STORE_DEV_K8S_CLUSTER_CERT }}
          k8s_namespace: ${{ secrets.AUTOGENERATED_NSCC_STORE_DEV_K8S_NAMESPACE }}
          k8s_token: ${{ secrets.AUTOGENERATED_NSCC_STORE_DEV_K8S_TOKEN }}
      - name: Result
        shell: bash
        run: echo ${{ steps.delete_dev.outputs.delete-message }}\
